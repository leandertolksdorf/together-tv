{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n '🤖 Express','⚛️  React  ' -c 'black.bgBlue','black.bgYellow' 'nodemon index.js' 'cd client && npm start'",
    "build": "cd ../client && npm install && npm run build",
    "start": "nodemon index.js",
    "heroku-postbuild": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.0.1",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "node-fetch": "^2.6.1",
    "nodemon": "^2.0.7",
    "ws": "^7.4.4"
  }
}
